<!DOCTYPE html>
<html>
<head>
  <title>E-Cart Dashboard</title>
</head>
<body>
  <h1>Welcome to the E-Cart Dashboard</h1>

  <h2>Products</h2>
  <% if (products && products.length > 0) { %>
    <ul>
      <% products.forEach((product) => { %>
        <li><%= product.name %></li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No products available</p>
  <% } %>
</body>
</html>
